export function getRandomInt(max) {
    /*
    Epreuve [2]
    TODO: Ajoutez à la place de "0" un nombre aléatoire entre 0 et "max":
    */
   return Math.floor(Math.random() * Math.floor(max));
}
var entier = getRandomInt(1, 15);